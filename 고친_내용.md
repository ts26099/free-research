# 물병던지기 시뮬레이션 — 고친 내용과 그 이유

원본 코드를 실제 물병·실제 물과 견주어 보고 고친 것들입니다.
결론부터: **가설은 대체로 맞습니다.** 다만 "액체의 종류·질량과 무관" 은
엄밀히는 틀리고, "**빈 병의 질량비 하나를 통해서만 관계된다**" 가 정확한
표현입니다. 아래 3번에 그 근거가 있습니다.

---

## 0. 원본 코드에서 가장 큰 문제: 연구 질문에 답할 수가 없었다

원본은 계산이 끝나면 **최종 각속도** 와 **몇 바퀴 돌았나** 를 알려 줍니다.
그런데 연구가 묻는 것은 "잘 세워지는가" 입니다. 회전이 느려지는 것은
세워지기 위한 **필요조건일 뿐** 이고, 그것만으로는 세워지지 않습니다.
병은 닿는 순간 똑바로 서 있어야 하고, 남은 회전이 넘어뜨릴 만큼 크면 안 됩니다.

그래서 착지 판정을 넣고, 거기서 연구용 측정 함수들을 만들었습니다.
(자세한 것은 5번)

---

## 1. 병 모양 — 원통이 아니라 진짜 PET 병

**원본:** 높이 0.28 m, 반지름 0.04 m 의 **닫힌 원통**. 부피가 **1.4 L** 입니다.
사람들이 던지는 병이 아니고, 무엇보다 **목이 없습니다.**

**고친 것:** 몸통 - 어깨 - 목으로 이어지는 회전면 `R(z)` 을 넣고,
기본값을 **500 mL 생수병** (508 mL, 높이 0.21 m, 몸통 반지름 0.031 m,
목 반지름 0.0105 m) 으로 바꿨습니다.
`neck_radius = bottle_radius` 로 두면 예전 원통으로 돌아갑니다.

**왜 중요한가 (이 연구에서 특히):**

- **충전율의 정의 자체가 달라집니다.** 충전율은 "병 부피 중 액체가 차지하는
  비율" 인데, 병 부피가 달라지면 같은 충전율이 다른 물기둥을 뜻합니다.
  원통이면 충전율 0.35 = 높이의 35% 지만, 실제 병에서는 부피의 대부분이
  몸통에 있어 **수면이 훨씬 낮습니다** (0.35 → 높이의 27%).
- **목이 물을 붙잡습니다.** 원심력에 밀린 물이 뚜껑 쪽으로 갈 때 좁은 목을
  지나야 합니다. 목의 부피는 9 mL 밖에 안 되므로, 물이 "뚜껑에 붙어 있다" 고
  해도 대부분은 어깨 아래 몸통에 있어야 합니다. 원통 모델은 이걸 못 봅니다.
- **어깨의 경사면이 물을 아래로 밀어냅니다.** 기울어진 벽의 법선은 축 방향
  성분을 갖습니다. 원통 벽에는 없는 힘이고, 물이 얼마나 퍼지는지를
  실제로 좌우합니다.

부피와 그 역함수는 수치적분이 아니라 **해석적으로** 넣었습니다
(원기둥 + 원뿔대 + 원기둥). 검산: 해석해와 수치적분이 상대오차 1e-11,
역함수 왕복 오차 1e-16 m.

### 곁들여: 빈 병의 질량 분포

**원본:** `m(R²/2 + L²/12)` — 균질한 원통 껍질. 질량중심은 높이의 절반.

**고친 것:** 질량이 **벽면 넓이에 비례해** 퍼져 있다고 보고, 바닥 원판과
뚜껑까지 더했습니다. 실제 병은 목이 좁아 거기 질량이 적고, 바닥은 두꺼워
무겁습니다. 그래서 질량중심이 절반(0.105 m)이 아니라 **0.087 m** 로 내려옵니다.
넘어지는지 서는지는 질량중심 높이가 정하므로 이건 그냥 넘길 수 없습니다.

---

## 2. 물리 오류 — 물이 갖고 있는 각운동량을 버리고 있었다

**원본:** `omega = L / J`. 즉 **각운동량이 전부 병의 회전에 있다** 고 가정.

그런데 원본 코드는 물이 병 안에서 움직여서 갖는 각운동량 `L_rel` 을
**이미 계산해서 출력하고 있었습니다.** 출력 문구가 이랬습니다:

```
relative L : 1.57e-03 kg m^2/s at most, 9.75% of the total (dropped by every model)
```

**전체의 10%** 입니다. 이건 근사가 아니라 **1차 오차** 입니다.

**고친 것:** `L_total = J·omega + L_rel` 에서 `omega = (L - L_rel) / J`.

- 원판(slices) 모델에서는 `L_rel` 이 **정확히 0** 입니다. 원판은 축 위에서
  축 방향으로만 움직이므로 `(r - r_cm) × v = 0`. 그래서 원판 모델은 원래
  맞았습니다.
- 조각·구슬 모델에서는 0 이 아니고, 그래서 틀렸던 것입니다.

**검산:** 고치기 전에는 보존량 검사가 `J·omega` 를 보고 있어서 어긋남이
2.5e-04 로 나왔습니다. 이제 세 모델 모두 **8.7e-19** (기계 정밀도) 로
보존되고, 세 모델의 회전수가 0.705 / 0.734 / 0.724 바퀴로 **4% 안에서 일치**
합니다.

---

## 3. 반발계수의 부호 — 조용한 버그

**원본:** 기본값 `restitution = -0.3` 이고, 벽 반사에서 속도에 그대로 곱합니다.
음수여야 속도가 뒤집힙니다.

문제는 **누구나 반발계수를 양수로 씁니다.** 양수를 넣으면 오류도 경고도 없이
물이 벽에서 튕기는 대신 **벽을 계속 밀고 들어갑니다.**

**고친 것:** 부호는 코드 안에서 붙입니다. 어느 쪽으로 넣어도 물리가 같습니다.
기본값도 `+0.3` 으로 바꿨습니다.

---

## 4. 비행 시간 — 던지는 세기와 따로 놀았다

**원본:** `t_max = 0.6 s` 고정. 던지는 세기(`omega_0`)를 아무리 바꿔도
공중에 있는 시간은 그대로입니다. 실제 던지기와 어긋납니다.

**고친 것:** `launch_speed` (위로 던져 올리는 속도) 와 `release_drop`
(놓은 자리보다 얼마나 아래에 떨어지는가) 에서 비행 시간을 구합니다:

```
t = (v + sqrt(v² + 2 g h)) / g
```

기본값 3.0 m/s, 0.1 m → 0.64 s. 실제 물병던지기와 맞습니다.
예전처럼 고정하고 싶으면 `flight="fixed"`, 낙하만 보려면 `flight="drop"`.

**왜 중요한가:** 성공하려면 "딱 한 바퀴" 를 돌아야 하는데, 그건 회전 속도와
비행 시간이 **함께** 정하는 조건입니다. 시간이 고정이면 이 축이 사라집니다.
실제로 계산해 보면 성공하는 던지기는 (각속도, 던짐 속도) 평면 위의
**띠** 이고, 세게 던져 올릴수록 더 천천히 돌려야 합니다.

---

## 5. 각도 약속과 착지 판정 (새로 넣은 것)

### 각도 약속

**원본:** `theta_0 = pi/2` 가 기본이고, `theta` 가 무엇에서 잰 각인지
문서에 없습니다. 중력 항을 보면 `theta = 0` 이 **거꾸로 선 병** 이었습니다.

**고친 것:** `theta` = 병 축이 **연직 위** 에서 벗어난 기울기.
`0` 이면 똑바로 서 있고, `pi` 면 거꾸로. 기본값 `0`.
중력 항의 부호도 세 모델 모두 여기 맞춰 고쳤습니다.

**검산:** 회전 없이 중력만 켜고 물을 뚜껑에 두면, 이제 물이
0.143 m → 0.028 m 로 **내려옵니다.** (고치기 전에는 올라갔습니다.)

### 착지 판정 — `landing_outcome()`

병은 바닥 접지 링의 모서리를 축으로 넘어갑니다. 그래서 서려면 둘 다 필요합니다:

1. 닿는 순간의 기울기가 **넘어가는 한계** 안에 있을 것
   (한계 = 질량중심이 모서리 바로 위에 오는 각도, 이 병에서 약 11~15°),
2. 남은 **회전에너지** 가 질량중심을 그 모서리 위로 들어올리지 못할 것.

접지 반지름은 몸통 반지름이 아니라 그 85% 로 잡았습니다 — PET 병 바닥은
평평하지 않고 꽃잎 모양이라 실제로 닿는 자리가 더 좁습니다 (`base_radius`).

**검산:** 회전 없이 여러 기울기로 놓아 보면 10° 까지 서고 20° 부터 넘어집니다
(한계 10.9°). 정적으로 맞습니다.

### 여기서 꼭 알아야 할 것: 물은 충돌에서 병과 따로 논다

처음에 이 판정을 넣었더니 **어떤 던지기도 성공하지 않았습니다.** 실제로는
되는데 말입니다. 이유를 따져 보니 이렇습니다.

바닥이 상에 닿는 충돌은 **몇 ms** 로 끝나는데, 안의 물이 반응하는 데는
**수십 ms** 가 걸립니다. 그래서 물이 갖고 있던 회전은 병을 넘어뜨리는
운동으로 **거의 넘어가지 않습니다.** 반면 병을 도로 눌러 세우는 무게는
물까지 포함한 전체 질량입니다.

**이 비대칭이 부분적으로 채운 병이 서는 이유입니다.**
(물을 얼려서 던지면 안 서는 것과 같은 얘기입니다.)

`water_coupling` 이 이걸 나타냅니다 (0 = 완전히 따로, 1 = 굳은 물체).
기본값 0.15. **이것이 이 모델의 유일한 맞춘 값** 이므로,
`coupling_sensitivity()` 로 이 값을 흔들었을 때 결론이 움직이는지
확인할 수 있게 해 두었습니다. (결과는 아래.)

---

## 6. 연구용 측정 함수 (새로 넣은 것)

### `tolerance_scan()` — "잘 세워진다" 를 재는 법

한 번의 던지기로는 답할 수 없습니다. 던지는 세기를 조금만 바꿔도 서기도 하고
넘어지기도 하니까요. **"잘 세워진다" = 성공하는 던지기의 범위가 넓다** 입니다.
사람은 매번 똑같이 던지지 못하기 때문입니다.

그래서 충전율마다 **세워지는 `omega_0` 의 폭** 을 잽니다.
격자를 훑는 게 아니라 **성패의 경계를 이분법으로 찾으므로**, 격자 간격에
답이 좌우되지 않습니다. (처음에 격자로 했더니 성공률이 0.5~2% 로 나와서
격자 잡음과 물리를 구별할 수 없었습니다.)

### `substance_scan()` — 가설을 직접 겨냥

충전율을 고정하고 액체만 바꿉니다. 병을 가볍게 한 경우도 함께 계산합니다.

### `research_summary()` / `--research`

위 셋을 차례로 돌리고 결론까지 출력합니다.

---

## 결과 — 연구 질문에 대한 답

### 물음 1. 어느 충전율이 가장 잘 세워지는가

| 충전율 | 0.15 | 0.20 | 0.25 | 0.30 | **0.35** | **0.40** | 0.45 | 0.50 | 0.60 | 0.70 |
|---|---|---|---|---|---|---|---|---|---|---|
| 성공 폭 [rad/s] | 0.00 | 0.30 | 0.78 | 0.93 | **0.99** | **1.01** | 0.98 | 0.92 | 0.78 | 0.61 |

- **최적: 0.35 ~ 0.45**, 0.15 이하에서는 **한 번도 서지 않습니다.**
- 던짐 속도를 2.5 / 3.0 / 3.5 / 4.0 m/s 로 바꿔도 **네 곡선 모두 같은 자리에서
  최대** 입니다.
- `water_coupling` 을 0.0 ~ 0.50 으로 흔들어도 최적값은 **0.35 ~ 0.40** 에
  머뭅니다. 즉 **결론이 그 가정에 기대고 있지 않습니다.**
  (결합도를 올릴수록 최적값이 0.35 쪽으로 내려가고 높은 충전율이 더 빨리
  나빠집니다 — 아래 단서와 같은 방향입니다.)
- 흥미로운 것: 절대 폭은 0.40 에서, **상대 폭**(폭 ÷ 알맞은 세기) 은 **0.25**
  에서 최대입니다. 사람의 던지기 오차가 비율에 가깝다고 보면 최적이 조금
  아래로 내려옵니다. 두 잣대를 합치면 **0.25 ~ 0.45**, 가운데가 대략 0.35.

**→ 가설(30~40%)은 이 계산이 지지합니다.**

한 가지 단서를 붙입니다. 거의 가득 찬 병은 물이 움직일 빈 공간이 없어
실제로는 굳은 물체에 가까워집니다 (`water_coupling` 이 1 로 갑니다).
이 계산은 결합도를 일정하게 두므로 **높은 충전율 쪽을 실제보다 후하게**
봅니다. 참 최적값은 여기 나온 값이거나 **그보다 조금 낮습니다.**
실험과 견줄 때 이 쪽을 눈여겨보세요.

### 물음 2. 액체의 종류·질량과 무관한가

충전율 0.35 고정, 액체만 바꿈:

| 액체 | 밀도 | 질량 | epsilon | 회전수 |
|---|---|---|---|---|
| 가벼운 기름 | 600 | 0.107 | 0.171 | 1.134 |
| 에탄올 | 789 | 0.140 | 0.136 | 1.029 |
| 물 | 1000 | 0.178 | 0.110 | 0.954 |
| 글리세린 | 1260 | 0.224 | 0.089 | 0.893 |
| 모래 | 1600 | 0.284 | 0.072 | 0.842 |

**→ 회전수가 30% 나 달라집니다. 가설은 엄밀히는 틀렸습니다.**

그런데 **병을 가볍게 하면** (`bottle_mass → 0`):

| 액체 | 밀도 | epsilon | 회전수 |
|---|---|---|---|
| 가벼운 기름 | 600 | 0.001 | 0.632 |
| 물 | 1000 | 0.001 | 0.631 |
| 모래 | 1600 | 0.000 | 0.630 |

**→ 편차가 0.3% 로 사라집니다.**

**이유 (이게 이 연구의 핵심입니다):**

자유비행에서 물 요소의 운동방정식은

```
r'' = omega² (r - r_cm) - (2/tau) r'
```

**질량도 밀도도 들어 있지 않습니다.** 각속도는 `omega = L/J` 인데, 물만
있다면 `L` 과 `J` 가 똑같이 질량에 비례해서 **약분됩니다.**

그러므로 액체가 결과에 들어오는 통로는 **딱 두 개** 뿐입니다:

1. **`epsilon = m_병 / (m_병 + m_액체)`** — 빈 병이 전체 질량에서 차지하는 몫.
   병은 액체와 달리 안에서 움직이지 않으므로 약분되지 않습니다.
2. **`tau`** — 점성이 정하는 감쇠 시간. 물처럼 묽은 액체에서는 비행 시간보다
   훨씬 길어 거의 관여하지 않습니다 (꿀이라면 다릅니다).

**따라서 정확한 진술은 이렇습니다:**

> 물병 세우기의 결과는 액체의 종류나 질량에 **직접** 의존하지 않는다.
> 오직 **부피 비율** 과, 빈 병의 질량비 **epsilon** 을 통해서만 관계된다.
> 병이 가벼울수록 (`epsilon → 0`) "부피 비율만이 정한다" 는 말이 정확해진다.

이건 원래 가설보다 **더 강하고 더 검증 가능한** 주장입니다. 실험으로
확인하려면: 같은 병에 물 대신 소금물·기름을 같은 **부피** 로 넣고 던져 보되,
`epsilon` 이 달라진다는 점을 계산에 넣고 견주면 됩니다.

---

## 7. 쓰는 법

```python
# 한 번 던지기 (착지 판정까지 나옵니다)
report(run(Parameters(water_mass=0.17, omega_0=23)))

# 연구 질문에 한 번에 답하기
research_summary()

# 개별 측정
scan = tolerance_scan();  print(format_tolerance(scan));  figure_tolerance(scan)
print(format_substance(substance_scan(fraction=0.35)))
print(format_coupling_sensitivity(coupling_sensitivity()))
```

명령행:

```bash
python 물병던지기_시뮬레이션.py --research            # 연구 요약 (약 3분)
python 물병던지기_시뮬레이션.py --research --thorough  # 촘촘히 (수 분)
python 물병던지기_시뮬레이션.py --water-mass 0.17
python 물병던지기_시뮬레이션.py --ui                  # 브라우저 화면
```

화면(`launch_ui()`)에도 **성공 범위 / Success basin** 과
**액체 바꾸기 / Change liquid** 버튼을 넣었습니다.

**스캔은 `slices` 모델로 하세요.** 원판은 충전율이 얼마든 같은 개수로
나뉘므로 곡선이 매끄럽습니다. `parcels` / `beads` 는 충전율을 조금 바꿀 때마다
격자에 놓이는 층수가 뚝뚝 달라져서 이산화 잡음이 섞입니다 (같은 계산을 조각
모델로 하면 최적값은 0.45 로 비슷하지만 곡선이 요동칩니다). 조각·구슬 모델은
**던지기 하나를 자세히 확인** 할 때 쓰는 것이 맞습니다.

---

## 8. 이 모델이 하지 않는 것 (실험과 견줄 때 유의점)

- **손에 있는 동안** 은 계산하지 않습니다. 놓기 전에 이미 물이 움직이고 있고
  중력도 아직 상쇄되지 않았는데, 여기서는 자유비행부터 봅니다.
- **공기 저항** 은 무시했습니다 (0.5 초 동안은 다른 것에 비해 작습니다).
- **벽 항력** 은 실제 경계층을 푼 것이 아니라 `-(2/tau) v` 라는 선형 근사이고,
  **자유표면** 을 따로 추적하지 않습니다.
- **착지는 충돌을 계산한 것이 아니라 에너지 판정** 입니다. `water_coupling` 과
  `impact_absorption` 이 그 가정을 담고 있으니, 결론을 말하기 전에 흔들어
  보세요 (`coupling_sensitivity()`).
- 병이 회전면(하나의 평면) 안에서만 돕니다. 실제 던지기의 비틀림은 없습니다.
