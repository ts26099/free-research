# 물병 낙하 시뮬레이션 — 고친 내용

**시나리오:** 물이 든 병을 **뚜껑이 아래로 가게 잡고, 천천히 밀면서 높은 곳에서
놓습니다.** 떨어지는 동안 물이 재분포하며 관성모멘트 `J` 가 커지고, 각운동량
보존에 따라 각속도 `ω` 가 떨어집니다.

**측정하는 것:** 원래 코드가 재던 것 그대로 — `J(t)`, `ω(t)`, 자세 `θ(t)`.
착지 판정은 하지 않습니다. 바닥에 닿는 순간 계산이 끝납니다.

---

## A. 시나리오를 낙하로 바꾼 것

| 항목 | 전 | 후 |
|---|---|---|
| 비행 시간 | `t_max = 0.6 s` 고정 | **낙하 높이**가 정함 `t = √(2h/g)`, 기본 1.5 m → 0.553 s |
| 초기 자세 `theta_0` | `pi/2` (문서 없음) | **`pi` = 뚜껑이 아래**, 잡고 있던 그대로 |
| 초기 각속도 `omega_0` | 20 rad/s (던지기) | **5 rad/s** (천천히 미는 정도) |
| 물의 처음 위치 | `water_at_top` | 그대로 — 뚜껑을 아래로 잡고 있었으니 물이 뚜껑에 얹혀 있는 게 맞습니다 |

`flight` 는 `"drop"`(기본) 과 `"fixed"` 두 가지입니다. `"fixed"` 로 두면 높이를
무시하고 `t_max` 를 씁니다.

**`theta` 약속:** 병 축(바닥→뚜껑)이 **연직 위**에서 벗어난 각. `0` = 똑바로 선
병, `pi` = 뚜껑이 아래. 원본은 이게 문서에 없었고, 중력 항의 부호를 보면
`theta = 0` 이 거꾸로 선 병이었습니다. 세 모델 모두 부호를 여기 맞춰 고쳤습니다.
**검산:** 회전 없이 중력만 켜고 물을 뚜껑에 두면 이제 물이 0.143 → 0.028 m 로
내려옵니다(전에는 올라갔습니다).

그래프의 첫 칸도 `θ/2π` 대신 **놓은 뒤 돈 바퀴 수**로 바꿨습니다. 뚜껑을 아래로
놓으면 `θ` 가 0.5 바퀴에서 시작해 읽기 나빴습니다.

---

## B. 물리 오류 — 물이 갖고 있는 각운동량을 버리고 있었다

**원본:** `omega = L / J`. 각운동량이 전부 병의 회전에 있다고 가정.

그런데 원본은 물이 병 안에서 움직여 갖는 `L_rel` 을 **이미 계산해서 출력하고
있었습니다**:

```
relative L : 1.57e-03 kg m^2/s at most, 9.75% of the total (dropped by every model)
```

**전체의 10%** 입니다. 근사가 아니라 1차 오차입니다.

**고친 것:** `L = J·omega + L_rel` → `omega = (L - L_rel) / J`.

- 원판(slices) 모델은 `L_rel` 이 **정확히 0** 입니다. 원판은 축 위에서 축 방향
  으로만 움직이므로 `(r - r_cm) × v = 0`. 원판 모델은 원래 맞았습니다.
- 조각·구슬 모델에서 0 이 아니고, 그래서 틀렸던 것입니다.

**검산:** 세 모델 모두 `J·ω + L_rel` 이 **2.2e-19** (기계 정밀도)로 보존됩니다.
기본 낙하에서 세 모델의 결과:

| 모델 | J 증가 | ω 최종 | 바퀴 수 |
|---|---|---|---|
| slices | 1.222x | 4.092 | 0.4089 |
| parcels | 1.313x | 3.820 | 0.3830 |
| beads | 1.241x | 4.027 | 0.4036 |

---

## C. 반발계수 부호 — 조용한 버그

**원본:** 기본값 `-0.3`. 벽 반사에서 속도에 **그대로** 곱합니다. 음수여야 뒤집힙니다.

문제는 누구나 반발계수를 양수로 씁니다. 양수를 넣으면 오류도 경고도 없이 물이
튕기는 대신 **벽을 계속 밀고 들어갑니다.**

**고친 것:** 부호는 코드 안에서 붙입니다. 어느 쪽으로 넣어도 물리가 같습니다.
기본값도 `+0.3`.

---

## D. 병 모양 — 원통이 아니라 PET 병

**원본:** 높이 0.28 m, 반지름 0.04 m 의 **닫힌 원통** (1.4 L). 목이 없습니다.

**고친 것:** 몸통 - 어깨 - 목 회전면 `R(z)`, 기본값 **500 mL 생수병**
(508 mL, 높이 0.21 m, 몸통 반지름 0.031 m, 목 반지름 0.0105 m).
`neck_radius = bottle_radius` 로 두면 예전 원통으로 돌아갑니다.

**충전율을 부피 비율로 재는 연구라면 병 모양이 그 정의의 일부입니다:**

- 원통이면 충전율 0.35 = 높이의 35% 지만, 실제 병은 부피 대부분이 몸통에 있어
  **수면이 훨씬 낮습니다** (0.35 → 높이의 27%).
- 목의 부피는 **9 mL** 뿐입니다. 물이 "뚜껑에 붙어 있다" 해도 대부분은 어깨 아래
  몸통에 있어야 합니다.
- 어깨의 **기울어진 벽**은 법선이 축 방향 성분을 갖습니다 — 물을 안쪽으로만이
  아니라 아래로도 밉니다. 원통 벽에는 없는 힘입니다.

부피와 역함수는 수치적분이 아니라 **해석적**입니다(원기둥+원뿔대+원기둥).
검산: 해석해 vs 수치적분 상대오차 1e-11, 역함수 왕복 오차 1e-16 m.

**원판을 부피 좌표에서 나눕니다.** 원판은 같은 **질량**을 갖는데, 단면이 변하면
그게 같은 두께가 아닙니다. 그래서 위치와 비압축성 제약을 `s = S(z)` (바닥부터의
누적 부피)에서 겁니다. 부피 좌표에서는 원판이 어디 있든 같은 폭을 차지하므로
원통일 때와 똑같은 투영으로 풀립니다. 목에 들어간 원판은 길게 늘어나고,
관성모멘트를 셀 때도 그 자리의 좁은 반지름을 씁니다.

**빈 병의 질량 분포:** 원본은 `m(R²/2 + L²/12)` — 균질한 원통 껍질, 질량중심이
높이의 절반. 실제 병은 목이 좁아 거기 질량이 적고 바닥은 두꺼워 무겁습니다.
벽면 넓이에 비례해 퍼뜨리고 바닥·뚜껑 원판을 더하니 질량중심이
0.105 m → **0.087 m** 로 내려옵니다.

---

## E. 결과가 보이는 범위 (실험 조건 잡을 때 참고)

원심력이 `ω²` 에 비례하므로, 너무 살살 밀면 낙하 시간 안에 물이 거의 안 움직여
`J` 가 변하지 않습니다.

**낙하 높이별** (충전율 0.335, `ω₀ = 5 rad/s`):

| h [m] | 0.5 | 1.0 | **1.5** | 2.0 | 3.0 | 5.0 |
|---|---|---|---|---|---|---|
| t [s] | 0.319 | 0.452 | **0.553** | 0.639 | 0.782 | 1.010 |
| J 증가 | 1.08x | 1.15x | **1.22x** | 1.29x | 1.41x | 1.64x |
| ω 감속 | 0.92 | 0.87 | **0.82** | 0.78 | 0.71 | 0.61 |

**미는 세기별** (h = 1.5 m):

| ω₀ [rad/s] | 1 | 2 | 3 | **5** | 8 | 12 | 20 |
|---|---|---|---|---|---|---|---|
| J 증가 | 1.01x | 1.02x | 1.06x | **1.22x** | 1.90x | 2.94x | 3.48x |
| ω 감속 | 0.99 | 0.98 | 0.95 | **0.82** | 0.53 | 0.34 | 0.29 |

`ω₀ ≤ 3 rad/s` 면 변화가 1~6% 라 Tracker 로 잡기 어렵습니다.
**`ω₀ ≳ 5 rad/s`(약 0.8 회전/초) 또는 더 높은 곳**에서 놓아야 효과가 뚜렷합니다.

충전율이 너무 낮으면 물기둥 전체가 질량중심 한쪽에 놓여 **재분포가 아예 일어나지
않고** `ω` 가 그대로입니다. 그때는 요약에 경고가 붙습니다 (0.05 에서 J 증가
1.000x).

---

## F. 걷어낸 것

착지 판정과 거기 딸린 것들을 전부 뺐습니다:
`landing_outcome`, `format_landing`, `water_coupling`, `impact_absorption`,
`base_radius`, `tolerance_scan`, `standing_window`, `coupling_sensitivity`,
`substance_scan`, `research_summary`, `--research`, 그리고 UI 의 관련 버튼들.

던지기용이던 `launch_speed` / `release_drop` 도 뺐습니다.

남은 것은 원본이 갖고 있던 비교 도구들입니다: **충전율 스캔**, **항별 비교**
(코리올리·오일러·비압축성을 하나씩 꺼 보기), **모델 비교**.

---

## G. 쓰는 법

```python
report(run(Parameters(water_mass=0.17, omega_0=5, drop_height=1.5)))
draw(run(Parameters(model="beads", n_beads=400)))

# 원본에 있던 비교 도구들
filling_scan(*default_values())      # 충전율별
force_comparison(*default_values())  # 항별
compare_models(*default_values())    # 모델별
```

```bash
python 물병던지기_시뮬레이션.py --drop-height 2.0 --water-mass 0.17
python 물병던지기_시뮬레이션.py --omega-0 8 --model beads
python 물병던지기_시뮬레이션.py --ui
python 물병던지기_시뮬레이션.py --help
```

**모델 고르기:** 충전율을 촘촘히 훑을 때는 `slices` 를 쓰세요. 원판은 충전율이
얼마든 같은 개수로 나뉘어 곡선이 매끄럽습니다. `parcels` / `beads` 는 충전율을
조금 바꿀 때마다 격자에 놓이는 층수가 뚝뚝 달라져 이산화 잡음이 섞입니다.
조각·구슬 모델은 **한 조건을 자세히 확인**할 때 쓰는 것이 맞습니다.

---

## H. 이 모델이 하지 않는 것

- 자유낙하만 봅니다. **놓는 순간**과 **착지**는 계산하지 않습니다.
- **공기 저항**은 무시했습니다 (0.5 초 동안은 다른 것에 비해 작습니다).
- **벽 항력**은 경계층을 푼 게 아니라 `-(2/tau) v` 선형 근사이고,
  **자유표면**을 따로 추적하지 않습니다.
- 병이 **한 평면 안에서만** 돕니다. 실제로 놓을 때 생기는 비틀림은 없습니다.
